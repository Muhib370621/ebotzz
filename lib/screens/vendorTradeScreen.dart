import 'package:flutter/material.dart';

class VendorTradeScreen extends StatelessWidget {
  const VendorTradeScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Vendor Trade Screen"),
      ),
    );
  }
}
