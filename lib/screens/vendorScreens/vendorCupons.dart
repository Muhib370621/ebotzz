import 'package:flutter/material.dart';

class VendorCupons extends StatelessWidget {
  const VendorCupons({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("VendorCupons "),
      ),
    );
  }
}
