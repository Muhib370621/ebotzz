import 'package:flutter/material.dart';

class VendorDeliveryTime extends StatelessWidget {
  const VendorDeliveryTime({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("VendorDeliveryTime "),
      ),
    );
  }
}
