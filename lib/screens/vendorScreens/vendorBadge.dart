import 'package:flutter/material.dart';

class VendorBadge extends StatelessWidget {
  const VendorBadge({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("VendorBadge "),
      ),
    );
  }
}
