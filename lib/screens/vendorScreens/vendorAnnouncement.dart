import 'package:flutter/material.dart';

class VendorAnnouncement extends StatelessWidget {
  const VendorAnnouncement({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Vendor Announcement "),
      ),
    );
  }
}
