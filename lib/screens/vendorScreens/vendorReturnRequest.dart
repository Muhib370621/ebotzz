import 'package:flutter/material.dart';

class VendorReturnRequest extends StatelessWidget {
  const VendorReturnRequest({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("VendorReturnRequest"),
      ),
    );
  }
}
