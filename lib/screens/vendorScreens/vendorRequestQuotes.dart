import 'package:flutter/material.dart';

class VendorRequestQoutes extends StatelessWidget {
  const VendorRequestQoutes({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("VendorRequestQoutes"),
      ),
    );
  }
}
