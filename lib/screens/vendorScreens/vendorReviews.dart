import 'package:flutter/material.dart';

class VendorReviews extends StatelessWidget {
  const VendorReviews({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("VendorReviews"),
      ),
    );
  }
}
