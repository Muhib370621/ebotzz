import 'package:flutter/material.dart';

class VendorAuction extends StatelessWidget {
  const VendorAuction({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("VendorAuction "),
      ),
    );
  }
}
