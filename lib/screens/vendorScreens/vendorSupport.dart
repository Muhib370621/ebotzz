import 'package:flutter/material.dart';

class VendorSupport extends StatelessWidget {
  const VendorSupport({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("VendorSupport"),
      ),
    );
  }
}
