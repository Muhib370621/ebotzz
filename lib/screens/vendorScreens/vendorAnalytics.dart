import 'package:flutter/material.dart';

class VendorAnalytics extends StatelessWidget {
  const VendorAnalytics({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Vendor Analytics "),
      ),
    );
  }
}
