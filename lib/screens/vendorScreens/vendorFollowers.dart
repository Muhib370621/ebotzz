import 'package:flutter/material.dart';

class VendorFollowers extends StatelessWidget {
  const VendorFollowers({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("VendorFollowers "),
      ),
    );
  }
}
