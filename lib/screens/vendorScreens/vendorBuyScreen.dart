import 'package:flutter/material.dart';

class VendorBuyScreen extends StatelessWidget {
  const VendorBuyScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Vendor Buy screen"),
      ),
    );
  }
}
