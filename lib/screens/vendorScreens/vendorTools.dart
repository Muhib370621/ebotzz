import 'package:flutter/material.dart';

class VendorTools extends StatelessWidget {
  const VendorTools({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("VendorTools"),
      ),
    );
  }
}
